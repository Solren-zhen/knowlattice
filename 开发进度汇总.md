# 晶格 · KnowLattice · 开发进度汇总与交接文档

> 更新日期：2026-08-30 · 本文件供下次会话/接手快速恢复上下文，配合 `开发计划.md` 阅读。

## 交接速览（先说重点）

- **立即上手**：`cd ~/Desktop/medvault-main && npm install && npm run dev`（或 `npm run build && npm run preview`）。`build` + `lint` 均通过。
- **当前状态**：功能闭环已完整（导入→整理→复习→带出→管理），主包已由 2.63MB 瘦到 **769KB**（重依赖按需加载）。品牌「晶格 · KnowLattice」，深色为 Night 色系。
- **两块待真机确认**（不影响主流程）：① PDF 对照的 **docx-preview** Word 版式渲染；② **编辑器内点 `[[双链]]` 跳转**（思维导图里的双链点选已验证可用）。
- **下一步首选**：图片附件导入（医学联合 1GB 图片）或 部署 GitHub Pages 发朋友试用。
- **关键依赖**：`react-pdf-viewer`、`pdfjs-dist@3`、`docx-preview`、`mammoth`、`xlsx`、`sql.js`、`mind-elixir`、`force-graph`、`ts-fsrs`、CodeMirror 6。（`markmap` 已弃用卸载。）
- **内部数据标识**一律未动（IndexedDB `medvault`、备份 `app:'medvault'`、`medvault-*` key）；仅显示层改名。

---

## 1. 项目现状

- **定位**：本地优先的个人知识库 / 学习笔记应用（React 19 + TypeScript + Vite 8 + CodeMirror 6），纯浏览器、零服务器。
- **品牌**：**晶格 · KnowLattice**（曾用 知医/拾光/知屿，均被否决；受众不限于医学生、不含"医"字、含英文）。
- **运行**：`npm install` → `npm run dev`（5173）｜`npm run build` / `npm run lint` 全绿｜`npm run preview`（生产构建，SW 已停用）。
- **数据存储**：IndexedDB `medvault`（笔记）；`medvault-pdfs`（最近 PDF）；localStorage（`medvault-srs/qbanks/mistakes/excerpts/recents/todos/days`）。

---

## 2. 本会话完成（重头戏）

### 品牌 / 设计
- **改名**：显示层全面换成「晶格 · KnowLattice」（title / 侧栏 / hero / manifest / README / 效果图 / .apkg 牌组与模型名「晶格」「晶格 Basic」/ 导出文件名 `lattice-*`）。内部 id 不动。
- **背景**：浅色 = 冷白高级纸 + 细纤维纹理 + **医学几何插画**（六边形分子簇 / 细胞圆点 / 医学加号 / 心电曲线，低饱和）；深色 = 冷色深纸 + 极淡紫/青辉光。去掉早期大色块极光与漂移动画层。
- **配色**：accent 全局换成紫罗兰（浅 `#6c5ce7`）/ 暖金（深 `#e0a45f`），并统一散落的 rgba 与按钮渐变。
- **图标**：应用图标用用户的「晶体网格」PNG（`public/icon*.png`，favicon + PWA 192/512）；应用内小 logo 复刻为晶体格纹 SVG。

### 新功能
- **思维导图**：`views/MindMapView.tsx`，引擎为 **Mind Elixir**（见 §5 详细）。把当前笔记 Markdown 自动渲染成可编辑的 XMind 式思维导图（双击改字 / Tab 子级 / Enter 同级 / Delete 删 / 拖拽 / 右键 / 撤销重做）；`[[双链]]` 节点点击直达对应笔记；保存写回 Markdown（保留 frontmatter）。入口：编辑器工具栏「🧠 思维导图」与预览底部悬浮按钮，均懒加载。注：早期用 markmap 的"XMind 式内联编辑"（见下两行）已整体被 mind-elixir 取代，保留作历史记录。
- **复习返回**：`ReviewView` header 增加「← 返回主页面」按钮（`onClose` 返回主界面）。
- **复习数据导入/导出**：`ReviewView` 增加「导出复习数据 (.json)」与「导入复习数据 (.json)」——导入支持整包备份（自动取 `.srs`）或单独复习状态，合并后刷新队列；`core/srs.ts` 加 `exportSrsJson`/`importSrsFromJson`，并**修复 `importSrsState` 的 bug**（ts-fsrs 的 `due` 是 Date、序列化为 ISO 字符串，旧校验要求 number 会拒绝，现改为 string/number 都接受，回环测试通过）。
- **主区视图切换**：`Workspace` 新增「编辑 / 预览 / 分屏」切换（`paneMode`，默认**编辑**=单栏源码+混合渲染，去掉编辑器+预览左右重复的布局；预览/分屏可随时切回）。
- **Bundle 瘦身**：把重依赖视图改**懒加载**（`GraphView`(force-graph) / `QuizView`(mammoth+xlsx) / `PdfSplitView`(react-pdf-viewer+pdfjs)，均 `<Suspense>`；MindMapView(three/markmap)/AnatomyViewer3D 原本已懒）。主 chunk **2.63MB → 769KB（gzip 782KB→274KB）**，约 -70%。
- **深色主题 → Night 色系**：`#080B10` 深蓝黑底 + `#60A5FA` 亮蓝主色 + 少量 `#A78BFA` 紫点缀（边框/思维导图双链节点/渐变尾），替换原先的暖灰/橙金深色。**深色背景也加了与浅色同款的医学几何花纹**（`public/background-dark.svg`：心电曲线/分子簇/细胞圆点/加号，全部紫+蓝），不再是只有光斑。
- **背景花纹统一紫色**：浅色 `public/background.svg` 与深色 `public/background-dark.svg` 里的心电曲线/六边形分子簇/细胞圆点/医学加号全部改为紫色（`#A78BFA` 系 `rgba(167,139,250,…)`），蓝色仅作细微光斑。
- **深色下黑字修复**：① 思维导图节点颜色随主题切换——深色用亮色系 `DARK_PAL`（`#8ea1ff/#a5b4ff/c3cbff/…`，wiki 用 `#C4A9FF`），黑底上不再黑字；② 知识图谱 `nodeLabel` hover 提示文字随主题变浅色（深色 `#e7edf6`）。
- **思维导图编辑去闪回**：编辑态下"输入文字不再整图重建"——图数据走 `treeRef` + 结构版本号 `treeRev`（仅加/删时重建），纯改字只更新输入框与数据，连续逐字不再闪。
- **思维导图 XMind 式增强 + 紫色点缀**：进入编辑**自动选中中心节点**；顶部**浮动工具栏**（＋子级/＋同级/删除）；点节点内联改字（节点样式输入框 + **紫色 `#A78BFA` 选中光环**）；Enter=同级、Tab=子级、**Backspace 空值删除**（删后选父级）；`[[双链]]` 子节点用紫色。紫色即"少量紫"元素（暗色也用紫作边框强调）。
- **待办清单 Todo**：`views/TodoView.tsx`，增/勾/删/过滤/清空完成，`medvault-todos`。
- **标签聚合页**：`views/TagBrowser.tsx`，标签云 → 点标签 → 列笔记 → 直达。
- **学习统计 / 打卡**：`views/Dashboard.tsx` + `core/stats.ts`；复习评卡/题库作答自动 `markStudy()`，显示连续天数与近 7 天柱状 + 复习/错题/题库/笔记/待办汇总。Dashboard 内有「📦 载入演示数据」（`core/demo.ts` seedDemo）。
- **真 .apkg 导出**：`core/anki.ts` `exportApkg()`（sql.js 建 collection.anki2 + jszip 打包），复习面板新增「导出 .apkg」；`.txt` 保留。（需在 Anki 实测）
- **题库导入增强**：QuizView 支持 **Word(.docx, mammoth)** 与 **Excel/CSV(SheetJS)**，`core/qbank.ts` 加 `parseQuestionsFromText`（含内联选项）/`rowsToQuestions`；识别失败回填输入框。

### PDF 对照重构（本会话重点）
- 左窗改 **react-pdf-viewer**（`@react-pdf-viewer/core` + `default-layout`）：渲染**真实页面**、可选中文本层、自带缩放/适应宽度/翻页/缩略图/搜索/全屏。
- **Word**（.docx）：`docx-preview` **还原原版排版**（段落/表格/图片/分页，可划选）。
- **PPT 已弃用**（浏览器端无法重排版式；建议另存为 PDF 再对照）。
- **交互**：划选文字 → **自动生成草稿到右侧**（320ms 去抖）；浮层仅保留「复制」；扫描版 → 「粘贴文字生成草稿」兜底。
- `pdfjs-dist` **6 → 3**（与 react-pdf-viewer 兼容），`pdfLib.ts` worker 路径改 `.js`。
- 示例：`public/sample-lecture.pdf`（中文讲义，pdf-lib 生成）、`public/sample-word.docx`（含标题/表格，供测 Word 版式）。

### 其他 / 修复
- **SW / 缓存"读到旧版本"**：早期 PWA Service Worker 静态资源"缓存优先" + 版本号固定 → 重建后浏览器一直用旧缓存。**已停用 SW 注册 + 新版自动注销旧 SW**（`main.tsx`）；`sw.js` 保留并提示"发布新版本务必递增 VERSION"。
- 清理：卸载 `pdf-lib`/`@pdf-lib/fontkit`（仅生成样例用，src 未引用）。

---

## 3. 数据现状

- **笔记**：桌面 `医学联合` 5227 篇 md（97 个学科目录，Obsidian 风格）已导入（IAB + 备份）；可用「学习统计 → 载入演示数据」体验。
- **备份**：`~/Desktop/医学联合-导入备份.json`（5227 全量）；`medvault-清空前备份.json`（旧 15 篇）。
- **未导入**：医学联合图片附件（约 1GB）——索引已排除非 md，导入不受影响。
- ⚠️ **浏览器存储隔离**：IAB 与用户 Chrome 各自独立。

---

## 4. 已知问题 / 待办注意

1. **docx-preview 实机渲染未可视化确认**：IAB 无法上传文件；`public/sample-word.docx` 已就位，请在**真实浏览器**里用「PDF / Word」导入验证 Word 版式与划选。
2. **.apkg 需在 Anki 实测**：collection.anki2 结构已用 sql.js 验证（col/notes/cards/revlog/graves、ver=11）；请在 Anki「文件→导入」试一次，有问题反馈。
3. **PDF 对照**：react-pdf-viewer 文本层已确认可选中；PPT 已移除（浏览器端无法重排版式）。
4. **SW 已禁用**：本地调试避免旧缓存；若部署到 GitHub Pages 想离线/可安装，重开注册并递增 `sw.js` 的 `VERSION`。
5. **KaTeX 数学公式渲染**：用户判断"没必要"（多数医学生打不来），**已跳过**。
6. 5227 篇下的复习/题库/错题本未做全量压力测试。
7. 技术债：`src/index.css` 里有早期 PDF 手绘（`.pdf-page`/`.pdf-textlayer`/`.pdf-doc-html`/`.pdf-doc-text`）等死样式，可后期清理。

---

## 5. 下次排期候选（按价值排序）

- [x] **思维导图编辑升级为 Mind Elixir**（已完成，用户认可）—— `views/MindMapView.tsx` 用 `SSShooter/mind-elixir-core`（★3153，MIT）：双击改字 / Tab 子级 / Enter 同级 / Delete 删 / 拖拽 / 右键菜单 / 撤销重做。md↔树转换用自研 `parseOutline`→`{topic,children}`，保留「保存并写回笔记」与 frontmatter。**已修多个坑**：CSS 用官方子路径 `mind-elixir/style.css`；节点 `expanded:true`；`init(data)` 读 `data.nodeData`（`{nodeData:{topic,children}}` 包装）；保存从 `getData().nodeData` 取树。**双链点击跳转**：① 思维导图——mind-elixir 节点类名是 **`.me-main`**（不是 `.node`），双链文本在 `.me-tpc`；点击捕获阶段识别 `[[name]]` → `handleOpenLink`（已验证：点「氧解离曲线」切到该笔记）。② 编辑器——`Editor` 用 CM6 `domEventHandlers.click` + `posAtCoords` 检测 `[[name]]` → `handleOpenLink`（逻辑同导图；本环境坐标点击复现不可靠，待真机确认）。
- [ ] **图片附件导入**（医学联合 png/jpg → vault，预览显示）——索引已排除非 md，可直接做
- [ ] **部署 GitHub Pages 发朋友试用**（manifest/SW 已就绪；决定是否启用 SW → 递增版本）
- [ ] **学习打卡细化**（打卡日历/周报；现有 streak + 近7天柱状）
- [ ] **标签编辑/重命名 / 导航增强**（标签聚合页已做，可扩展按源/章节筛选）
- [ ] **WebDAV 同步**（坚果云免费版，零成本）
- [ ] **Tauri 打包**（Windows NSIS 0 元；先验证朋友愿长期用）
- [ ] **AI 讲义转草稿接真 LLM**（可选；需 API key）

---

## 6. 技术备忘

- **关键依赖**：`react-pdf-viewer/core`+`default-layout`、`pdfjs-dist@3`（worker `?url`）、`docx-preview`、`mammoth`、`xlsx`(SheetJS 官源)、`sql.js`+`@types/sql.js`、`mind-elixir`、`force-graph`、`ts-fsrs`、`jszip`、`idb`、`minisearch`、`markdown-it`、CodeMirror 6 系列。（注：`markmap-lib/view` 已随思维导图引擎更换而卸载。）
- **架构**：`core/`（vault/parser/linkIndex/searchIndex/srs/mistakes/qbank/noteGen/pdfLib/anki/stats/demo/tooltip/livePreview）、`storage/`（web/tauri）、`views/`（Workspace + 各功能视图 + Todo/TagBrowser/Dashboard）。
- **性能要点**：搜索索引懒构建+增量（`VaultSearch`）、链接索引单篇增量、frontmatter `metaCache`、`readAll` 单遍读、树虚拟滚动（26px 行高）、导入导出走内存 + 批量写入。
- **PDF 对照**：react-pdf-viewer 渲染原版；docx-preview 还原 Word；划选→自动成稿。
- **验证套路**：`npm run build` + `npm run lint`；浏览器 DOM 断言 + 截图（IAB 截图偶尔失败、标签不稳定 → 多用 DOM 断言、必要时新开标签）。

---

## 7. 一句话总结

产品闭环完整：**导入**（PDF/Word/题库/粘贴/md 文件夹）→ **整理**（智能草稿/PDF 对照/标签聚合/图谱）→ **复习**（FSRS/题库/错题/待办/打卡）→ **带出**（备份 / Anki .txt/.apkg / md 文件夹 .zip）→ **管理**（批量删除/导出）。下一个里程碑：**图片附件导入 + 发朋友试用**。
