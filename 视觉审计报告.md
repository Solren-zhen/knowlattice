# 视觉审计报告 · 晶格 KnowLattice

> 阶段：Step 2（审计交付物）。**本文件不含任何代码改动**——按约定在此停下，等你裁决后再动手。
> 范围：18 个界面 × 浅色/深色 = 36 个状态。
> 审计时间点：`git` 基线提交 `78f8310`，工作树干净。

---

## 0. 先声明四件事（其中三件是对我自己的更正）

### 0.1 我看不了图

我当前的模型**不具备图像输入能力**（`read_image` 直接拒绝）。所以原计划里「我截图然后自己看」这一步**作废**。

36 张截图已经生成，是**给你看的**，不是给我的：`.shots/before/<界面>.<主题>.png`。

我的全部结论来自四条不依赖眼睛的证据链：

| 证据链 | 手段 | 产物 |
|---|---|---|
| A 静态结构 | 解析 `src/index.css` 文本 | `.shots/audit/css-audit.json` |
| B 运行时测量 | CDP 注入测量脚本（对比度/溢出/点击区/计算样式） | `.shots/audit/runtime-audit.json` |
| C 渲染事实 | `CSS.getPlatformFontsForNode` 等 CDP 域 | 见 §4 |
| D 控制台 | `Runtime.consoleAPICalled` / `exceptionThrown` | `.shots/before/manifest.json` |

**这决定了分工**：客观缺陷（对比度、层级冲突、一致性）我能给硬数字和行号;「好不好看 / 有没有高级感」这一层**必须你目视**。我不会替你对审美下判断。

### 0.2 我推翻了自己一个中间结论（点击区）

首轮测量我按 **32×32px** 阈值统计，得到「每屏 44~83 个小点击区」，一度准备写成问题。

改用 WCAG 2.2 AA 的 **24×24px** 下限重新统计后：**不合规 0 类**。只有 5 类落在 24~32px（`rail-btn` 30×30、`.new-btn` 71×30、`.tree-row` 222×26），桌面端合理。

**这条从缺陷清单里删除。**后文所有数字都按 24px 阈值。

### 0.3 我推翻了自己另一个中间结论（溢出）

首轮报告「`18-brain` 溢出 56 处」。去重后实际是 **1 个真问题**：`.brain-name` 内容宽 244px 被压进 174px 容器（`overflow:hidden` 裁掉右侧）。其余 55 条是同一问题在不同列表项上的重复计数。

**报告按去重后的 1 处计。**

---

### 0.4 我推翻了第三个结论：token 层其实是健康的，P0 的主题也报反了

这两条是复核阶段发现的，**都会改变实施动作**，所以必须留在报告里：

**（a）「77 处 token 定义有 28 处冗余」是假的。**
我的解析器只把 `@media` 这类 `@` 开头的规则当作作用域，**把 `[data-theme='dark']` 当成了普通规则**，于是深色主题那 28 处合法覆盖被统计成了「浅色作用域内重复定义」。

实测：浅色 `:root` **48 个 token，自身零重复**；深色 **28 处覆盖，全部合法**；77 = 48 + 28 + 1（`--arrow-left` 单例）。

> ⚠️ **如果按原方案执行「77 处收敛到 49 处」，会把整个深色主题删掉。**这条动作已作废。

**（b）P0 的主题报反了，根因也报错了。**详见 §3.2（已重写）。

教训记在这里：**静态解析的「作用域」判断必须显式覆盖所有主题选择器，不能只认 `@`**；对比度结论**必须带主题归属**，否则会把浅色的毛病和深色的毛病混成一条。

---

## 1. 一句话诊断

**问题不是「不够好看」，是三层叠加造成的系统性不一致。**

> **深色主题像是被认真设计过的；浅色主题像是被补丁推出来的。**

支撑这判断的最硬一个数字：**浅色主题 209 处对比度失败，深色主题 19 处——差 11 倍。**同样一套组件、同样的 CSS，只因为主题切换就退化一个数量级，说明浅色不是设计出来的，是「顺便能跑」的。

---

## 2. 证据链 A：CSS 静态结构（「叠加腐化」的量化）

文件 `src/index.css`，2546 行，28 个分节。

| 指标 | 实测 | 说明 |
|---|---|---|
| 规则块 / 声明 / 唯一选择器 | 688 / 2755 / 639 | 体量本身不小 |
| **同一选择器被重复定义** | **39 个选择器，散布在 88 个规则块中** | 后写的覆盖先写的，谁赢取决于书写顺序 |
| 完全重复的声明（同选择器+同属性+同值） | 17 条 | 纯死重量，删掉零影响 |
| **同选择器+同属性但值不同** | **27 条** | 真正的危险区：改动其中一处不生效 |
| token 定义处数 / 名字数 | 77 / 48 | 48 浅色 + 28 深色覆盖 + 1 单例，**无冗余**（原判有误，见 §0.4） |
| 引用了但从未定义的 token | **0** | ✅ 无静默失效 |
| 定义了但从未引用的 token | 1（`--r-xl: 18px`，第 62 行） | 死 token |
| 浅色 `:root` 内部重复定义 | **0 个** | 原报 28 个是解析器把深色块误当浅色作用域（见 §0.4） |
| `var(--*)` 引用 | 810 处，分布在 604 行 | 改 token **值**可零风险传播 |
| 非 4px 栅格的间距值 | **68 处** | 无间距纪律 |
| `!important` | 4 处 | 尚可控 |

### 2.1 重复定义的具体坐标（抽样）

```
x6  from                    行 287, 524, 713, 805, 956, 1245
x6  to                      行 288, 524, 714, 806, 957, 1247
x2  :root                   行 6, 2418
x2  .btn-small.saved        行 158, 1252
x2  .btn-primary            行 170, 2072
x2  .btn-primary:hover      行 180, 2083
x2  .sidebar-header         行 207, 2102
x2  .tree-row               行 322, 2028
x2  .tree-row.file.active   行 334, 2030
x2  .editor-toolbar         行 348, 2098
```

**`.btn-primary` 被定义两次**（170 / 2072），而 `background` `border-radius` `font-size` `box-shadow` `transition` 五项全部在两次之间**取值不同**——第 2072 行那一版实际上完全接管了它。你在 170 行读到的代码是**死代码，但看起来像活的**。这是这个文件最危险的一类腐化。

### 2.2 这个文件是怎么变成这样的

文件尾部叠了约 10 层「追加覆盖」区块，每一层都在修上一层的显示问题：

```
行 2070  yunwu.ai 细节仿制（追加覆盖，安全可回退）
行 2411  unified feature panel skeleton (impeccable layout)
行 2337  深色主题（深邃生物舱 Deep Navy）
行 840   M5 解剖图谱
```

**这些层的注释本身写着「安全可回退」——说明作者知道这是债。**所以 §6 的核心动作不是「重新设计」，而是**把这些层合并回它本该在的位置**，让同一件事只有一个定义。

---

## 3. 证据链 B：运行时对比度（最严重的问题所在）

方法：遍历每个可见文本节点，沿祖先链合成真实背景色（含 rgba 叠加），按 WCAG 公式算对比度。正文需 ≥4.5:1，大字（≥24px，或 ≥18.66px 且 bold）需 ≥3:1。

### 3.1 逐界面统计

| 界面 | 浅色失败数 | 深色失败数 | 溢出 |
|---|---|---|---|
| 01 首页空状态 | 6 | 0 | 0 |
| 02 编辑态工作台 | 11 | 0 | 0 |
| 03 快速搜索 | 11 | 0 | 0 |
| 04 复习模式 | 12 | 0 | 0 |
| 05 题库练习 | 11 | 0 | 0 |
| 06 错题本 | 11 | 0 | 0 |
| 07 标签聚合 | 12 | 1 | 0 |
| 08 待办清单 | 14 | 2 | 0 |
| 09 学习统计 | 12 | 1 | 0 |
| 10 知识图谱 | 12 | 1 | 0 |
| 11 思维导图 | 11 | 0 | 0 |
| 12 历史版本 | 11 | 0 | 0 |
| 13 格式转换 | 12 | 1 | 0 |
| 14 智能草稿 | 11 | 0 | 0 |
| 15 PDF 对照 | 13 | 3 | 0 |
| 16 AI 助手侧栏 | 12 | 1 | 0 |
| 17 3D 解剖图谱 | **16** | **9** | 0 |
| 18 脑图谱 | 11 | 0 | **56→1** |
| **合计** | **209** | **19** | **1** |

去重后共 **41 类**不同的对比度失败。

### 3.2 P0 —— 解剖界面在「深色」主题下几乎不可读 ⚠️ 本条已更正

**我最初写的是「浅色主题」，方向是反的。**逐元素 dump 计算色 + 祖先背景链之后，真实情况如下：

| 主题 | 文字 | 背景 | 结果 |
|---|---|---|---|
| 浅色 | `rgb(51,65,85)` = `--text-2` | `rgba(255,255,255,0.85)` | ✅ 正常 |
| **深色** | `rgb(203,213,225)` = `--text-2` | **`rgba(255,255,255,0.85)`** | ❌ **1.07:1 隐形** |

**根因不是我说的「边框色被当文字色」，而是：这几个类的背景是硬编码的 `rgba(255,255,255,0.85)`，不跟随主题；而文字色取自 `var(--text-2)`，会跟随主题在深色下翻成浅灰。浅灰字压白芯片 = 隐形。**

命中清单（全部是 3D 画布**外面**的 HTML 文本层，不碰 WebGL）：

```
.sys-chip                  系统芯片            1.07:1
.tool-chip                 视角/旋转工具芯片    1.07:1
.viewer3d-legend .lg       动脉/静脉/神经/淋巴  1.07:1
.viewer3d-hint             底部提示语           1.96:1
.graph-legend .lg-item     知识图谱图例         1.07:1
```

修复：这 5 处硬编码白底统一换成已有的 `var(--panel-glass-strong)`（浅色≈白、深色≈墨），文字与底色同时跟随主题，语义与实现都正确。另把 `.pdf-right`（**滚动区域**）的 `backdrop-filter` 去掉改为实色——履行「模糊只用于静态层」的约定。

> ⚠️ **与你「3D 解剖模型不要变」的边界不冲突**：以上全是画布外的 HTML 文本层，渲染层一行未动。

> 📌 **这条更正的价值**：如果没做主题归因，我会去改浅色主题（那里本来是好的），而深色继续坏着。

### 3.3 P1 —— 三个系统性对比度缺陷（改 3 个 token 值即可）

这三条不是散点，是**复用模式本身不达标**，所以出现在 17~18 个界面上。

#### 缺陷 P1-1：`--accent` 当文字色用在浅色底上（17 个界面）

```
实测 4.04:1（需 4.5）   前景 #6366f1 = --accent   背景 #f3f3fe
命中（每屏都有）：
  .tree-row.file.active      树里当前选中的笔记
  span.lp-wiki               编辑器里的 [[双链]]
  span.badge-exam            考试重点徽章
  button.btn-small.saved     已保存按钮
  button.todo-filter.on      待办筛选选中态
```

**精算修复方案**：直接把 `--accent` 加深一档（同一色相，Indigo 500→600），一处改动同时解决「当文字色」与「当填充色」两种用法：

```
#6366f1 on #f3f3fe = 4.04:1   ✗（原）
#4f46e5 on #f3f3fe = 5.71:1   ✓（新 --accent）
白字 on #6366f1   = 4.47:1   ✗（原，差 0.03）
白字 on #4f46e5   = 6.29:1   ✓（新）
```

`--accent-hover`、`--accent-soft`、`--accent-ring`、`--border-accent`、`--glow-accent` 同步换到新色相。

#### 缺陷 P1-2：白字压在「深色主题」的主色上（2.98:1，需 4.5）⚠️ 主题已更正

```
实测 2.98:1   前景 #ffffff   背景 #818cf8
命中：button.sys-chip.on / button.tool-chip.on / button.pdf-tab.on / button.todo-check.on
```

深色主题的 `--accent` 是 `#818cf8`（浅靛），白字压上去只有 2.98:1。**这是深色专属问题**（浅色那批是 4.47:1，同属 P1-1）。

`.on` 芯片的文字色被硬编码成 `color: #fff`，所以换不掉。修复方式是补一个语义 token `--accent-ink`（主色填充之上的文字色）：

```
浅色 --accent-ink: #ffffff   → 白字 on #4f46e5 = 6.29:1  ✓
深色 --accent-ink: #10162b   → 深墨 on #818cf8 = 6.02:1  ✓
```

这也顺带说明：`.btn-primary` 在深色下早就在用「浅靛底 + 深墨字」（`#10162b`），只是这个做法没有被抽象成 token，所以 `.on` 芯片没跟上。

#### 缺陷 P1-3：`--muted` 当正文色（18 个界面）

```
实测 4.34:1（#64748b on #f1f5f9）—— 差 0.16
其它落点：#f3f3fe 上 4.31 / #e8e5de 上 3.78
命中：.dir-count（目录条目数）、.rail-font-glyph、.dash-label、.tag-chip span.muted …
```

`--muted` 在**白底上是合格的**（4.76:1）——所以它是「在白底上设计、被搬到灰底上就失效」。

```
#64748b on #f1f5f9 = 4.34:1   ✗（现状）
#475569 on #f1f5f9 = 6.92:1   ✓（slate-600）
```

### 3.4 一个反直觉的发现

**主按钮自己不达标。**原 `--accent: #6366f1` 上的白字 = **4.47:1**，差 0.03。

0.03 在实际观感上不可见。但这条说明**这套色板是在「刚好卡线」的位置选的**——任何一处轻微的底色偏移都会把它推过线，这正是 §3.3 那三条系统性失败的成因。

既然 `--accent` 本来就要为 P1-1 加深一档，这 0.03 顺带被解决（白字 on `#4f46e5` = 6.29:1），不需要为它单独做取舍。

---

## 4. 证据链 C：其它渲染事实

### 4.1 字体：Windows 实测解析结果（零外部字体，符合约定）

CDP `CSS.getPlatformFontsForNode` 实测（UA: `Windows NT 10.0; Win64; x64`）：

| 元素 | 实际用到的字体 |
|---|---|
| `.tree-row.file` | `Segoe UI`(3 字形) + `Microsoft YaHei`(5 字形) |
| `button.btn-primary` | `Segoe UI Semibold`(6) + `Microsoft YaHei`(4) |
| `p` | `Segoe UI`(16) + `Microsoft YaHei`(29) |
| `h1`（欢迎页大标题） | **`Microsoft YaHei`(11) 纯中文** |
| `kbd` | `Segoe UI Semibold`(6) |

**结论：字体栈按设计工作。**拉丁走 Segoe UI，中日韩走 Microsoft YaHei，**无任何网络字体请求**。

声明的字体栈（`index.css:36`）：

```css
-apple-system, 'PingFang SC', 'Segoe UI', 'Microsoft YaHei', 'Noto Sans CJK SC', sans-serif
```

⚠️ 注意：前两位 `-apple-system` / `PingFang SC` 在 Windows 上不解析、无害；**但我无法在此验证 macOS 上的实际表现**——你已接受的这个风险仍然成立，我只能保证「不溢出、不重叠」这类结构性问题跨平台成立，不能保证同样好看。

### 4.2 字号：CSS 里 24 种，实际渲染 17 种

```
CSS 声明值（24 种）：0.9em 0.92em 0.95em 9px 10px 10.5px 11px 11.5px 12px 12.5px 13px
                     13.5px 14px 14.5px 15px 16px 16.5px 17.5px 20px 22px 23px 24px 28px 56px
实测渲染值（17 种）：10px 10.5px 11px 11.5px 12px 12.5px 13px 13.3333px 13.5px
                     14px 14.5px 15px 16px 20px 24px 25px 28px
```

会出现 `13.3333px` 说明有 `em` 或百分比在嵌套计算——**这是没有字阶的直接后果**。一个 Operate 模式的工具型界面，合理字阶是 **6 档**（如 11/12/13/14/16/20）。

**但我要克制**：把 17 种收敛到 6 种会改变每一屏的视觉重量，这属于「精修」还是「重设计」的边界地带。见 §9 待裁决。

### 4.3 层级：19 个不同的 z-index，含 999 / 998 / 997

```
实测值：60 100 90 80 5 6 999 998 997 85 2 88 86 87 84 30 1 -1 3
```

出现 999/998/997 说明**有人用「加个更大的数」来修遮挡问题**——这正是 §2.2 那种「追加覆盖」在层级维度的同一种病。

### 4.4 溢出与裁切：真实缺陷 **0 处** ⚠️ 本条已更正

我最初报了「18-brain 有 1 处裁切」，**复核后撤回**：`.brain-name`（`index.css:2517`）同时具备 `overflow:hidden` + `text-overflow:ellipsis` + `white-space:nowrap`，`scrollWidth > clientWidth` 正是省略号截断的**必要条件**，不是缺陷。

我的检测器把「内容超出隐藏盒子」一律判为裁切，因此**所有正常的省略号标签都会被误报**——56 处计数全部来自这一个原因。

**结论：18 个界面、两种主题，溢出与裁切维度的真实缺陷为 0。**

### 4.5 点击区：全部合规

```
低于 24×24（WCAG 2.2 AA 不达标）：0 类
24~32 之间（桌面端可接受）：5 类
  30×30   .header-actions > button.btn-icon（更多操作）
  71×30   .sidebar-header > button.new-btn（新建）
  222×26  .tree-row.dir / .tree-row.file
```

**建议保持现状**——桌面端 26px 行高是密度上的正确取舍，为了凑 44px 而加大会直接损害你「日常使用效率」的第一诉求。

### 4.6 焦点指示：**发现一个真实缺陷**

全局焦点环是存在的（`index.css:95`）：

```css
:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
```

但 `.qs-input`（快速搜索输入框，第 716 行）写着：

```css
.qs-input { border: none; outline: none; ... }   /* 第 718 行 */
```

**两者权重相同（都是 0,1,0），而 718 行声明在后 → 全局焦点环被覆盖。**并且全文件搜索确认：`.qs-input` **没有任何 `:focus` / `:focus-visible` 规则**。

→ **快速搜索输入框完全没有焦点指示**（WCAG 2.4.7 不达标）。同一问题在 `.anatomy-search`（967 行）上是弱化版：只剩 1px 下边框变色。

---

## 5. 证据链 D：控制台

36 个状态里捕获到的全部告警：

| 次数 | 内容 | 定性 |
|---|---|---|
| **38×** | `Invalid DOM property 'stop-color'. Did you mean 'stopColor'?` | React DOM 属性名写错 |
| **2×** | `思维导图初始化失败：TypeError: Cannot read properties of undefined (reading 'querySelector')` | 见下，**不是缺陷** |
| 2× | `niivue-warn textHeight is deprecated` | 脑图谱用了废弃 API |
| 1× | `THREE.WebGLProgram: Program Info Log: warning X4122 ...` | 3D 着色器警告（不碰） |

### 5.1 `stop-color` —— 确定的一处缺陷

`src/views/icons.tsx:212-213`：

```jsx
<stop offset="0" stop-color="#4b3fd8" />
<stop offset="1" stop-color="#8a6bff" />
```

React 要求驼峰式 → 应为 `stopColor`。每屏都刷，是导航时最显眼的噪音源。

### 5.2 思维导图报错 —— **我先说错过，这里更正**

我一度据控制台判定「思维导图整屏不可用」。**探针实测证明这是错的**：

```
MINDMAP DOM 实测：.map-canvas=1  .me-root=1  .me-main=2  .me-tpc=6  bodyHTML=12177
节点文本完整可见（「动作电位 / 分期: 去极化 → 反极化 → 复极化 …」）
```

**界面渲染是正常的。**那个 TypeError 来自一个**已被销毁的实例**：

```
at ir.init      (mind-elixir.js:2757) → ir.linkDiv (1485)
库内 MindElixir.js:1394  this.map.querySelector('.me-root')  →  this.map 为 undefined
```

成因：`main.tsx:28` 开着 `StrictMode`，React 19 开发环境双调用 effect → effect#1 的实例被 cleanup 里的 `mind.destroy()` 销毁（`MindElixir.js:2651` 清空 `this.map`），**而它那个浮动 Promise 的 `init` 还在跑** → 撞上销毁后的空状态。effect#2 的实例正常渲染。

**定性：健壮性问题，不是视觉缺陷；生产构建（无 StrictMode 双调用）不会触发。**归入 §7，本次不动。

### 5.3 一条性能观察

`17-anatomy` 打开时 `document.body` 文本量 = **70210 字符**（其他界面 900~1300）。结构条目一次性全量渲染进 DOM，无虚拟化。这是可感知的卡顿来源，归入 §7。

---

## 6. 缺陷清单（按严重度）与拟改动清单

### 6.1 缺陷清单

| 编号 | 级别 | 缺陷 | 证据 | 位置 |
|---|---|---|---|---|
| **D1** | **P0** | 解剖/图谱界面的芯片与图例在**深色**主题下不可读（1.07:1）——5 处硬编码白底不跟随主题 | 逐元素计算色 + 祖先背景链取证 | `index.css` 864/888/936/1051/1072/1518（HTML 文本层，非 WebGL） |
| **D2** | **P1** | `.qs-input` 完全无焦点指示 | 权重+顺序分析 + 全文件无 `:focus` 规则 | `index.css:718` |
| **D3** | **P1** | `--accent` 作文字色，17 个界面 4.04:1 不达标 | 运行时实测 | token `:root:22` + 各使用点 |
| **D4** | **P1** | `.on` 芯片白字压深色主色 `#818cf8` 仅 2.98:1，且文字色被硬编码为 `#fff` 无法换 | 运行时实测（带主题归属） | `index.css:875, 899` |
| **D5** | **P1** | `--muted` 作正文色，18 个界面 4.34:1 不达标 | 运行时实测 | token `:root:19` |
| **D6** | P1 | `.btn-primary` 等 39 个选择器被重复定义，其中 27 条同属性冲突——**170 行是死代码但看起来是活的** | 静态解析 | 全文，见 §2.1 |
| ~~**D7**~~ | ~~P2~~ | ~~token 冗余~~ **已作废**——48+28 是正常的主题设计，唯一的真问题是 `--r-xl` 死 token | 复核后重算 | `index.css:62` |
| **D8** | P2 | 24 种字号 / 15 种圆角 / 19 种 z-index（含 999/998/997）/ 127 个硬编码颜色 / 35 个硬编码阴影 / 68 处非 4px 间距 | 静态解析 | 全文 |
| ~~**D9**~~ | ~~P2~~ | ~~`.brain-name` 被裁切~~ **已作废**——该元素有 `text-overflow: ellipsis`，这是**正确的省略号截断**，不是缺陷 | 复核 `index.css:2517` | — |
| **D10** | P2 | `stop-color` 应为 `stopColor`，每屏刷 38 条告警 | 控制台 | `src/views/icons.tsx:212-213` |
| **D11** | P2 | `.anatomy-search` 焦点指示仅剩 1px 下边框变色 | 权重+顺序分析 | `index.css:967, 975` |

### 6.2 拟改动清单（对应的动作）

| 动作 | 针对 | 风险 |
|---|---|---|
| ~~A. 收敛 token 定义~~ **作废**（见 §0.4）。改为：删掉死 token `--r-xl`，新增 1 个语义 token `--accent-ink` | D7 | 极低 |
| **B. 合并尾部 10 层追加覆盖**，把 `.btn-primary` 等重复定义合并为单一定义 | D6 | 低——合并而非改写，逐条比对后删除被完全覆盖的旧版 |
| **C. 修 `.qs-input` / `.anatomy-search` 焦点环** | D2, D11 | 极低——纯 CSS |
| **D. 修系统性对比度**：`--accent` 加深一档（`#6366f1`→`#4f46e5`）、`--muted`→`#475569`、新增 `--accent-ink` 供 `.on` 芯片 | D3, D4, D5 | 低——只改 token 值，形态语言不变 |
| **E. 把 5 处硬编码白底换成 `var(--panel-glass-strong)`**（仅 HTML 文本层） | D1 | 低——换颜色来源，不动布局 |
| **F. 收敛层级体系**，给 19 个 z-index 建立 5~6 档 | D8 | 中——需逐层核对遮挡关系 |
| **G. 建立字阶**（若你批准，见 §9） | D8 | 中高——会改变每一屏的视觉重量 |
| **H. 修 `.brain-name` 裁切** | D9 | 低 |

### 6.3 我会怎么验证（不靠我的眼睛）

每一批改完后重跑同一套测量，用**改动前后的数字对比**代替「我觉得好看了」：

```
浅色对比度失败 209 → 目标 < 20
深色对比度失败  19 → 目标 < 10
重复选择器     39 → 目标 0
冲突声明       27 → 目标 0
token 定义     77 → 目标 49
字号种数       17 → 见 §9 裁决
溢出            1 → 目标 0
控制台 stop-color 告警 38 → 0
```

外加 36 张 after 截图与 before **并排**交给你——**审美这关由你过，不由我过**。

---

## 7. 功能与交互发现 · 本次不实现

> 按约定：这些是**功能性/健壮性问题，不是视觉问题**，本次一行都不动，只登记。

### F1. 思维导图：开发环境每次打开抛一次 TypeError

**现象**：`StrictMode` 双调用 + 浮动 Promise 竞态，被销毁实例的 `init` 仍在执行，在 `MindElixir.js:1485` 抛错。界面本身渲染正常，生产构建不受影响。

**建议**：`MindMapView.tsx:99` 的 `mind.init(data).catch(...)` 是浮动 Promise；cleanup（第 101 行）里 `mind.destroy()` 前需要一个「已取消」标志，让迟到的 `init` 静默退出。

**为什么本次不动**：改它要碰 effect 的生命周期逻辑，属于你划的「功能」范围。

### F2. 脑图谱用了废弃 API

`niivue-warn textHeight is deprecated (use fontMinPx and fontSizeScaling)`。当前版本仍工作，但升级 niivue 时会断。

### F3. 解剖结构列表全量渲染

78 个系统 / 大量结构条目一次性进 DOM（body 文本 70210 字符，是普通界面的 60 倍）。打开解剖界面的可感知延迟主要来自这里。虚拟滚动能解决，但那是重构。

### F4. AI 助手是外站 iframe

`AiPanel.tsx` 用 `<iframe>` 嵌 Kimi / 智谱清言 / 通义千问 / 腾讯元宝 / DeepSeek。**headless 截图中必然空白**——这不是缺陷，是外站拒绝被嵌或需要登录。**你需要在真实浏览器里自己看这一屏。**

### F5. 思维导图依赖预发布版

`package.json` 锁定 `mind-elixir: 6.0.0-next.4`（预发布）。当前工作正常，但预发布版无兼容性承诺——F1 那个报错就发生在库内部。长期建议评估降到 5.x 稳定版或跟进 v6 正式版。

### F6. three.js 着色器警告

`THREE.WebGLProgram: Program Info Log: warning X4122: sum of 0.996094 and -2.98545e-017 cannot be ...`——一个浮点精度警告，不影响渲染。**属于你锁定的 WebGL 层，本次不碰。**

---

## 8. 边界确认（我理解的、你划的红线）

| 边界 | 我的执行方式 |
|---|---|
| **3D 解剖模型不变** | 冻结 WebGL 层：模型、材质、灯光、相机、选中高亮、颜色算法一律不动。**但画布外的 HTML 层**（顶栏、搜索框、系统树、结构列表、图例、空态、滚动条）属于外观，会改——D1 就落在这里 |
| **只改外观和布局，不动功能** | 可以改 CSS 全部 + JSX 结构与层级；**不改任何 state / 事件处理 / 数据流 / 副作用**。D10（`stop-color`→`stopColor`）是唯一的 JSX 属性改动，属渲染正确性，**需要你点头** |
| **可用性缺陷算外观** | D2、D11、D9 属于此类，已纳入实施范围 |
| 第三方库 | CodeMirror / react-pdf-viewer / mind-elixir / force-graph 只通过本项目已覆盖的钩子改，不深穿皮肤 |
| 零外部字体 | 已实测确认，保持 |
| 桌面优先 ≥1280 | 不做窄屏/移动端适配 |
| 浅色+深色都是一等交付物 | 每屏双截图，两种主题都进回归 |
| 玻璃拟态 | 保留，但 `backdrop-filter` 只用于静态层，**绝不用在滚动区域** |
| 不改的东西 | 文案、WebGL 内部、`src-tauri/`、vault/storage/parser 重构、30 个测试文件、新增依赖、外部资源、手工改 `dist/` |

---

## 9. 需要你裁决的三件事

开工前有三个判断题需要你定，因为它们决定的是「精修」还是「重设计」。

> **执行后的更新**：Q3 你已批准并落地（`--muted → #475569`）。**Q1 与 Q2 我执行时改判了**——拿到逐条归属数据后，我原方案的前提不成立，两条都已撤回，理由见 **§10.4**。本节保留原始判断，供你对照。

### Q1. 字阶要不要收敛？（D8）

现状 17 种渲染字号（含 `13.3333px` 这种计算产物）。

- **选项 A（推荐）**：收敛到 6 档（11 / 12 / 13 / 14 / 16 / 20）。收益是一致性质变，**代价是每一屏的文字视觉重量都会变**，你会感觉「整个界面被重新排过一次」。
- **选项 B**：只清理 12.5 / 13.5 / 14.5 / 16.5 / 17.5 这 5 个「半像素档」，收敛到 12 档。改动温和，保留现有节奏，但一致性只解决一半。

### Q2. 层级体系要不要重建？（D8）

19 个 z-index，含 999/998/997。重建为 6 档（如 base 1 / 面板 10 / 抽屉 20 / 模态 30 / 浮层 40 / 提示 50）能让遮挡关系可推理，**但任何一处判断失误都会造成新的遮挡 bug**，需要你接受一次「可能有一屏层级要返工」的风险。

### Q3. `--muted` 变深，你接受吗？（D5）

`--muted` 从 `#64748b` 提到 `#475569` 才能过 4.5:1。**代价是次级文字会明显变重**，界面从「轻、透气」偏向「实、密」。

这是**唯一一处「修正确性」和「保你的既有观感」直接冲突**的地方——`--muted` 是你现在的视觉语言里承担「次要信息」的主要手段，动了它，整体气质会朝 Linear 那种更硬的方向偏一点。

- **选项 A（推荐）**：改。正确性优先，且你选的 Linear 锚点本身就是更硬的气质。
- **选项 B**：折中到 `#5b6b80`（约 5.4:1），保留一部分轻盈感。
- **选项 C**：不改 `--muted`，改为**只在 `--panel` / `--panel-deep` 灰底上**局部加深。规则更复杂，但白色底上的观感完全不变。

---

## 10. 第一批执行结果（正确性修复，已提交 `ef48cd9`）

范围严格限定在**颜色值与两处焦点环**，未动布局、未动结构、未动功能。

### 10.1 实测：改前 → 改后

| 指标 | 改前 | 改后 | 怎么测的 |
|---|---|---|---|
| 对比度失败（浅色） | **209** | **0** | 18 界面 × 运行时计算色 + 7 级祖先背景链 |
| 对比度失败（深色） | **19** | **0** | 同上 |
| 控制台告警 | **43** | **5** | 每屏收集，去重计数 |
| 溢出裁切真实缺陷 | 0 | 0 | 去重后（§4.4，原报 1 处系误报） |
| 点击区不合规 | 0 | 0 | WCAG 2.2 AA 24×24 |
| 单元测试 | 201 通过 | 201 通过 | 24 个测试文件 |

### 10.2 改了什么

```
index.css  :root    --accent  #6366f1 → #4f46e5   （当文字色 4.04→5.71，当填充色 4.47→6.29）
                    --muted   #64748b → #475569   （4.34→6.92）
                    新增 --accent-ink（主色填充上的文字色）
index.css  [dark]   --muted   #7c8aa5 → #8b98b0   （在 accent 染色底上 4.3→5.1）
                    新增 --accent-ink: #10162b
index.css  864/888/936/1051/1072/1518
                    硬编码 rgba(255,255,255,.85) → var(--panel-glass-strong)   ← P0 根因
index.css  875/899/1414   .on 芯片 color:#fff → var(--accent-ink)
index.css  1407     .todo-clear 补 background/border 重置（此前一直在渲染浏览器原生按钮底）
index.css  1950     .pdf-right 去掉 backdrop-filter（滚动区域约定）
index.css  718/975  补 .qs-input / .anatomy-search 的 :focus-visible
icons.tsx  212-213 stop-color → stopColor
```

### 10.3 附带的两个发现（不是我有意去找的）

**（a）`.todo-clear` 一直在显示 Chrome 的原生按钮样式。** 全文件只有 `button { font-family: inherit }` 这一条 button 规则，`.todo-clear` 没有重置 `background`/`border`，于是它渲染成系统默认的灰色胶囊。浅色下是 `#f0f0f0`，深色下是 `#6b6b6b`——深色下配 `--muted` 文字只有 **1.53:1**。

**（b）`.pdf-right` 是滚动容器（`overflow-y: auto`）却带了 `backdrop-filter: blur(12px)`。** 这违反你定的「模糊只用于静态层」，已改实色。

### 10.4 我**没有**做的三件事，以及为什么

这三条都是原计划里的动作，撤回或改判的原因是**执行中拿到的数据推翻了立项时的前提**。

#### （1）C2「合并尾部 10 层追加覆盖」——降级，只做能证明安全的 5 处

我写了个脚本逐条比对「先写块的每条声明是否被后写块覆盖」。结果：

```
重复选择器 39 个
  ├ 可安全整块删除（被完全覆盖）: 5 个
  └ 不能删（先写块有独占声明）: 34 个
```

例如 `.sidebar-header`：先写块（209 行）有 `display:flex` / `align-items` / `justify-content` / `gap`，后写块（2108 行）只覆盖了 `background`。**整块删掉会把顶栏布局打散。**

所以这不是「删掉旧的叠加层」，是 34 组需要逐条求并集的迁移。它**不产生任何视觉变化**（后写块本来就赢），纯粹是维护性收益，而我又无法目视验证——风险收益比不成立。**建议等你能配合看图时再做。**

#### （2）C5「字阶收敛到 6 档」——撤回，理由是我原来的方案算错了

拿到逐条归属后发现问题不在「数字多」，而在**同一个值在不同位置必须往相反方向走**：

```
.lp-h3（编辑器内联渲染）  14.5px → 必须变 15，否则与 14px 的 .lp-h4 撞成同一级
.markdown-body（欢迎页）  14.5px → 必须变 14，否则与 15px 的 h3 撞成同一级
```

一个机械的「值 → 档位」映射会把这两处层级**直接压平**。要做对，得对 176 处声明逐点做语义判断，而其中 36 处的改动量是 **±0.5px（肉眼不可见）**。

同时 6 档方案本身有问题：它要求 `14.5→16`、`16.5→20`、`22→28` 这类 **>20% 的跳变**——那是在破坏设计，不是收敛。**这条撤回**，改为「等能看图时，按语义角色重排」。

#### （3）C4「z-index 收敛到 6 档」——未执行

19 个值里最可疑的是 `80 / 84 / 85 / 86 / 87 / 88 / 90` 这 7 个挤在 10 以内、全部服务于全屏浮层。它们**很可能**是互斥的（一次只开一个），合并成 1~2 档大概率安全——但「大概率」不是「可证明」，而判断失误的后果是遮挡 bug，且我无法目视验证。

### 10.5 边界确认

- WebGL 渲染层：**一字未动**（27–141 行的颜色函数、202–966 行的 three.js 初始化全部原样）
- 状态 / 事件处理 / 数据流 / 副作用：**一字未动**
- 唯一改到的 `.tsx` 是 `icons.tsx` 的两个 SVG 属性名（`stop-color` → `stopColor`），语义等价，消除 React DOM 告警
- 未新增依赖、未引入外部资源、未编辑 `dist/`、未触碰 30 个测试文件中的任何一个

### 10.6 你怎么验收

```
.shots/compare.html   ← 用浏览器打开，36 组「改前 / 改后」并排，点图可放大
```

每组是同一界面、同一主题、同一组确定性种子数据，A/B 唯一变量就是这次的样式改动。**深色主题下的解剖图谱、知识图谱图例、待办清单、标签聚合、学习统计是变化最明显的几屏**——那些正是原来 1.07:1 ~ 1.53:1 的地方。

> 我看不了图（`deepseek-v4.1-flash` 不接受图像输入，已实测确认），所以上面每一个数字都来自程序化测量，每一个「好看了」的判断都得由你的眼睛做。

---

## 附：本次审计的产物清单

```
.shots/before/                  36 张基线截图（18 界面 × 浅/深）+ manifest.json
.shots/after/                   36 张改后截图 + manifest.json
.shots/compare.html            改前/改后并排对照页（浏览器打开）
.shots/audit/css-audit.json    CSS 静态解析全量结果
.shots/audit/runtime-audit.json 运行时测量全量结果（含每屏明细）
.shots/harness.mjs             截图 harness（零依赖 CDP）
.shots/runtime-audit.mjs       运行时审计 harness
.shots/measure.js              浏览器侧测量脚本
.shots/demo.mjs                确定性种子数据（31 笔记 / 30 题 / 12 SRS 卡 / 8 错题 / 8 待办 / 30 天）
.shots/css-audit.mjs           静态审计脚本
.shots/derive.mjs              统计派生脚本
.shots/refactor-plan.mjs        重复选择器合并安全性分析（§10.4）
.shots/offscreen.mjs            出屏元素归因脚本
```

> `.shots/` 已在 `.gitignore` 中排除，不会污染你的仓库。
